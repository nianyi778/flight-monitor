services:
  flight-monitor:
    image: ghcr.io/nianyi778/flight-monitor:v4.6
    # 本地构建时改用: build: .
    container_name: flight-monitor
    restart: unless-stopped
    ports:
      - "127.0.0.1:8081:8081"
    environment:
      # Telegram
      - TG_BOT_TOKEN=${TG_BOT_TOKEN}
      - TG_CHAT_ID=${TG_CHAT_ID}
      - TG_GROUP_IDS=${TG_GROUP_IDS:-}
      # 数据库
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT:-4000}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME:-flight_monitor}
      # 调度
      - CHECK_INTERVAL=${CHECK_INTERVAL:-3600}
      - PUSH_INTERVAL=${PUSH_INTERVAL:-3600}
      # 代理（可选）
      - PROXY_URL=${PROXY_URL:-}
      # 携程 DOM 抓取：宿主机 Chrome CDP 端口（Docker 内通过 host.docker.internal 访问）
      # 宿主机启动: google-chrome --remote-debugging-port=9222 --user-data-dir=/tmp/cdp
      - CTRIP_CDP_PORT=${CTRIP_CDP_PORT:-host.docker.internal:9222}
      # MCP Server
      - MCP_ENABLED=${MCP_ENABLED:-true}
      - MCP_PORT=${MCP_PORT:-8081}
    volumes:
      - flight-data:/app/data
    deploy:
      resources:
        limits:
          memory: 512M

volumes:
  flight-data:
